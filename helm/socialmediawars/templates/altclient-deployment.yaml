apiVersion: apps/v1
kind: Deployment
metadata:
  name: socialmediawars-altclient
  labels:
    {{- include "socialmediawars.labels" . | nindent 4 }}
    app.kubernetes.io/name: socialmediawars-altclient
spec:
  selector:
    matchLabels:
      {{- include "socialmediawars.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/name: socialmediawars-altclient
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "socialmediawars.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/name: socialmediawars-altclient
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}          
          image: "{{ .Values.altclient.image.repository }}:{{ .Values.altclient.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.altclient.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP          
          resources:
            {{- toYaml .Values.resources | nindent 12 }}      
